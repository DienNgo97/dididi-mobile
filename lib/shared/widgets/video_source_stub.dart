import 'dart:typed_data';

import 'package:video_player/video_player.dart';

/// Tạo VideoPlayerController từ bytes đã tải (auth). Bản stub (không nền tảng nào khớp).
Future<VideoPlayerController> controllerFromBytes(Uint8List bytes, String contentType) {
  throw UnsupportedError('Không hỗ trợ phát video trên nền tảng này');
}
